export const styles = theme => ({
  button: {
    marginBottom: theme.spacing.unit,
    marginRight: theme.spacing.unit,
  },
  dialogTitle: {
    textAlign: 'center',
  },
  dialogTextField: {
    width: '100%',
  },
});
